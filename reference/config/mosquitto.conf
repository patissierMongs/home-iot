# Mosquitto MQTT Broker 설정
# 실행: mosquitto -c config/mosquitto.conf

# 외부 기기(Qingping 등)가 접속할 수 있도록 모든 인터페이스에서 리슨
listener 1883 0.0.0.0
allow_anonymous true

# 로깅
log_type all
log_dest stdout

# 메시지 지속성
persistence true
persistence_location /tmp/mosquitto/
